// Convert functions to one-line arrow functions.

function greet(name) {
  return `Hello ${name}!`;
}

const greet = (name) => `Hello ${name}!`;

function add(a, b) {
  return a + b;
}

const add = (a, b) => a + b;
